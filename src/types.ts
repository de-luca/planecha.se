declare interface Coordinates {
  x: number;
  y: number;
}

declare interface Passivity {
  passive?: boolean;
  initiator?: string;
}

declare enum Theme {
  SYS = 'sys',
  DRK = 'drk',
  LGT = 'lgt',
}
